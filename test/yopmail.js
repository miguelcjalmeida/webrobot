var should = require('chai').should(),
    yopmail = require('../yopmail'),
    createEmail = yopmail.createEmail;

describe('#createEmail', function() {
  it('gets the created email', function() {
  
  });
});
